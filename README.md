Vaadin with Immediate Field Validator example
=========================

This is a simple example that shows how to use immediate text fields and
delayed validation errors with them

To run application call

mvn jetty:run
